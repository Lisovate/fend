# @fendsh/cli-linux-x64

Native Linux x64 binary package for `@fendsh/cli`.

This package contains:

- the Linux host `fend` binary
- the packaged `fendd` guest agent used to build the Linux runtime on first run

It is installed automatically as an optional dependency of `@fendsh/cli` on
supported Linux x64 hosts.
